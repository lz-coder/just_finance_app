import 'package:flutter/material.dart';

class HomePageGraphics extends StatelessWidget {
  const HomePageGraphics({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.pie_chart);
  }
}
